# OneDrive-backed folders can fail hardlinks on Windows.
# Force pnpm to copy packages instead of hardlinking.
package-import-method=copy

# Also avoid pnpm's default isolated linker layout inside OneDrive.
node-linker=hoisted
